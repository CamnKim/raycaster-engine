import { TILE_SIZE } from '../../constants/constants';
import map, { mapWidth } from '../../constants/map';
import Player from '../../types/Player';
import { WebGLContext } from '../../types/types';
import round from '../math/round';
import drawLine from './drawLine';

const dda = (player: Player) => {

};

const castRays = (glContext: WebGLContext, player: Player) => {

};

export default castRays;
